#include <stdio.h>



int main(){
	//needs to call for untapping call cards that can be untapped modifiers
	//needs to call for upkeep flags to be resolved 
	//needs to call for card draw(can you/will die?) + moddifiers

	//Call for all cards in active players battlefield to be untapped if table to

	//Call for upkeep flags to be resolved

	//increase hand by pulling from library 



	return 0;
}
